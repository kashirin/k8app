import React, { Component } from "react";

class RedButton extends Component {
    constructor(props) {
        super(props);
    }
    
    render() {
        return <button style={{color:'red'}}>Жми</button>;
    }
}

export default RedButton;
